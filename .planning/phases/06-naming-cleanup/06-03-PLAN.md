---
phase: 06-naming-cleanup
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - .planning/config.json
  - .planning/PROJECT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/**/*-SUMMARY.md
  - .planning/phases/**/*-PLAN.md
  - .planning/phases/**/*-CONTEXT.md
  - .planning/phases/**/*-RESEARCH.md
autonomous: true

must_haves:
  truths:
    - "No 'spotlighter' references exist in planning documentation"
    - "Project name references updated to 'md-quick-look' or 'MD Quick Look'"
    - "Historical summaries reference new naming for searchability"
  artifacts:
    - path: ".planning/config.json"
      provides: "Project configuration with new name"
      contains: "md-quick-look"
    - path: ".planning/PROJECT.md"
      provides: "Project overview with updated references"
      contains: "MD Quick Look"
    - path: ".planning/STATE.md"
      provides: "Current state with updated naming"
      contains: "md-quick-look"
  key_links:
    - from: ".planning/config.json"
      to: "project references"
      via: "project_name field"
      pattern: "md-quick-look"
---

<objective>
Update all planning documentation to remove legacy "spotlighter" references and establish consistent "MD Quick Look" naming throughout.

Purpose: Complete the NAMING-02 requirement by ensuring documentation is consistent with the codebase rename. This enables grep-ability and presents a unified identity for the project.

Output: All 85+ planning documentation files updated with consistent naming; no "spotlighter" references remain in documentation.
</objective>

<execution_context>
@/Users/razielpanic/.claude/get-shit-done/workflows/execute-plan.md
@/Users/razielpanic/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-naming-cleanup/06-CONTEXT.md
@.planning/phases/06-naming-cleanup/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update core planning documents with new naming</name>
  <files>
    .planning/config.json
    .planning/PROJECT.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
    Update the core planning documents with consistent naming:

    **config.json:**
    - `"project_name": "md-quick-look"` -> `"project_name": "md-quick-look"`

    **PROJECT.md, STATE.md, REQUIREMENTS.md:**
    - Replace `md-quick-look` with `md-quick-look` (kebab-case for repo/directory references)
    - Replace `MD Quick Look` with `MD Quick Look` (display name references)
    - Replace `spotlighter` with `MDQuickLook` or `MD Quick Look` as appropriate for context
    - Preserve git history references where they describe past state (e.g., "renamed from...")

    Use case-insensitive search to catch all variations: `spotlighter`, `Spotlighter`, `SPOTLIGHTER`.

    DO NOT update:
    - Commit messages or changelog entries describing historical changes
    - Any references that should document the transition (e.g., "renamed from md-quick-look")
  </action>
  <verify>
    - `grep -i "spotlighter" .planning/config.json` returns no matches
    - `grep -i "spotlighter" .planning/PROJECT.md` returns no matches (except historical context)
    - `grep -i "spotlighter" .planning/STATE.md` returns no matches
    - `grep "md-quick-look" .planning/config.json` returns match
  </verify>
  <done>
    Core planning documents updated with consistent "md-quick-look" and "MD Quick Look" naming.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update phase documentation across all phases</name>
  <files>
    .planning/phases/**/*-SUMMARY.md
    .planning/phases/**/*-PLAN.md
    .planning/phases/**/*-CONTEXT.md
    .planning/phases/**/*-RESEARCH.md
    .planning/phases/**/*-VERIFICATION.md
  </files>
  <action>
    Batch update all phase documentation files:

    **Search and replace patterns (case-insensitive where noted):**
    1. `md-quick-look` -> `md-quick-look` (repo name, directory references)
    2. `md_quick_look` -> `md_quick_look` (underscore variants if any)
    3. `MD Quick Look` -> `MD Quick Look` (display name)
    4. `MD-Quick-Look` -> `MD-Quick-Look` (if any hyphenated display names)
    5. `spotlighter` (standalone, case-insensitive) -> appropriate variant based on context

    **Context-aware replacements:**
    - In Swift/code examples: use `MDQuickLook`
    - In bundle ID contexts: use `com.rocketpop.MDQuickLook`
    - In user-facing/display contexts: use `MD Quick Look`
    - In file path/directory contexts: use `MDQuickLook` or `md-quick-look`

    **Files to update (found via grep):**
    ```bash
    grep -ril "spotlighter" .planning/phases/
    ```

    Process each file, making appropriate replacements based on context.

    Commit with message:
    ```
    docs(06): update planning documentation to MD Quick Look naming

    - Update 85+ documentation files with consistent naming
    - Replace md-quick-look with md-quick-look throughout
    - Replace MD Quick Look with MD Quick Look in display contexts
    - Preserves historical context in transition documentation
    ```
  </action>
  <verify>
    - `grep -ri "spotlighter" .planning/phases/` returns no matches (except intentional historical references)
    - `grep -ri "md-quick-look\|MD Quick Look\|MDQuickLook" .planning/phases/` returns many matches
    - `git log -1 --oneline` shows documentation update commit
  </verify>
  <done>
    All phase documentation updated with consistent naming. No unintentional "spotlighter" references remain.
  </done>
</task>

</tasks>

<verification>
**Final verification checklist:**

1. **No spotlighter references in planning docs:**
   - `grep -ri "spotlighter" .planning/` returns no matches (or only intentional historical context)

2. **Core config updated:**
   - `.planning/config.json` shows `"project_name": "md-quick-look"`

3. **Consistent naming throughout:**
   - Grep for new naming returns matches across all planning docs
   - Display name "MD Quick Look" used consistently

4. **Documentation committed:**
   - `git log -1` shows documentation update commit
</verification>

<success_criteria>
- Zero unintentional "spotlighter" references in .planning/ directory
- config.json project_name updated to "md-quick-look"
- All v1.0 phase summaries reference new naming
- NAMING-02 requirement fully satisfied
- All changes committed atomically
</success_criteria>

<output>
After completion, create `.planning/phases/06-naming-cleanup/06-03-SUMMARY.md`
</output>
